---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---

<!doctype html>
<html lang="en" data-theme="holoseduca">
  <head>
    <BaseHead
      title="Contact HolosEduca"
      description="Get in touch with HolosEduca - Together we can transform education in Colombia"
    />
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-grow container mx-auto px-4 py-8">
      <div class="hero bg-base-200 rounded-box p-8 mb-12" data-aos="fade-down">
        <div class="hero-content text-center">
          <div>
            <h1 class="text-5xl font-bold mb-8">Contáctanos</h1>
            <p class="text-xl">
              Tienes preguntas o quieres colaborar? Nos encantaría escuchar de
              ti.
            </p>
          </div>
        </div>
      </div>

      <div class="grid lg:grid-cols-1 max-w-2xl mx-auto gap-8">
        <div
          class="card bg-base-100 shadow-xl border border-zinc-100"
          data-aos="fade-right"
        >
          <div class="card-body">
            <h2 class="card-title text-2xl mb-6">Envíanos un mensaje</h2>
            <form class="space-y-8" id="contact-form">
              <div class="form-control">
                <label class="label" for="name">
                  <span class="label-text">Nombre</span>
                </label>
                <input
                  type="text"
                  id="name"
                  placeholder="Tu nombre"
                  class="input input-bordered w-full"
                  required
                />
              </div>

              <div class="form-control">
                <label class="label" for="email">
                  <span class="label-text">Correo electrónico</span>
                </label>
                <input
                  type="email"
                  id="email"
                  placeholder="tu.email@example.com"
                  class="input input-bordered w-full"
                  required
                />
              </div>

              <div class="form-control">
                <label class="label" for="country">
                  <span class="label-text">País</span>
                </label>
                <select id="country" class="select select-bordered w-full">
                  <option>Colombia</option>
                  <option>Argentina</option>
                  <option>Chile</option>
                  <option>Mexico</option>
                  <option>Peru</option>
                  <option>Otro</option>
                </select>
              </div>

              <div class="form-control">
                <label class="label" for="subject">
                  <span class="label-text">Asunto</span>
                </label>
                <select
                  id="subject"
                  class="select select-bordered w-full"
                  required
                >
                  <option value="">Selecciona un asunto</option>
                  <option value="collaboration"
                    >Oportunidad de colaboración</option
                  >
                  <option value="volunteer">Interés en voluntariado</option>
                  <option value="donation">Donación</option>
                  <option value="support">Apoyo educativo</option>
                  <option value="other">Otro</option>
                </select>
              </div>

              <div class="form-control">
                <label class="label" for="message">
                  <span class="label-text">Mensaje</span>
                </label>
                <textarea
                  id="message"
                  class="textarea textarea-bordered h-32 w-full"
                  placeholder="Tu mensaje aquí..."
                  required></textarea>
              </div>

              <div class="form-control mt-6">
                <button type="submit" class="btn btn-neutral w-full btn-lg"
                  >Enviar por WhatsApp</button
                >
              </div>
            </form>
          </div>
        </div>
      </div>
    </main>
    <Footer />

    <script>
      const form = document.getElementById("contact-form");
      form?.addEventListener("submit", (e) => {
        e.preventDefault();

        const nameInput = document.getElementById("name") as HTMLInputElement;
        const emailInput = document.getElementById("email") as HTMLInputElement;
        const countryInput = document.getElementById(
          "country",
        ) as HTMLSelectElement;
        const subjectInput = document.getElementById(
          "subject",
        ) as HTMLSelectElement;
        const messageInput = document.getElementById(
          "message",
        ) as HTMLTextAreaElement;

        const name = nameInput.value;
        const email = emailInput.value;
        const country = countryInput.value;
        const subject = subjectInput.value;
        const message = messageInput.value;

        const whatsappNumber = "573216193708";
        let whatsappMessage = `Hola, mi nombre es ${name}.`;

        if (email) {
          whatsappMessage += `\nMi correo es: ${email}.`;
        }

        if (country) {
          whatsappMessage += `\nPaís: ${country}.`;
        }

        if (subject) {
          const subjectText =
            subjectInput.options[subjectInput.selectedIndex].text;
          whatsappMessage += `\n\nAsunto: ${subjectText}.`;
        }

        if (message) {
          whatsappMessage += `\n\nMensaje: ${message}`;
        }

        const whatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(
          whatsappMessage,
        )}`;

        window.open(whatsappUrl, "_blank");

        (e.target as HTMLFormElement).reset();
      });
    </script>
  </body>
</html>
